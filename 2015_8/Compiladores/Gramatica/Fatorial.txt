begin
	int n;
	int fatorial;

	output("Insira o valor para calcular seu fatoria:");
	input(n);

	fatorial = 1;
	
	if(farotial <= 1)?
		output("Fatorial calculado:");
		output(farotial);
	@
		do:
			fatorail = fatorail * n;
			n = n - 1;
		done(n > 1);
	?;
end