Tipo de variavel --> Numerico


Entrada de dados --> input


Saida de dados --> output


Estrutura condicional --> if(condiçaõ)? se verdadeiro @ se falso?


Estrutura de repetição --> do: code done(condição)


Estrutura de repetição 2 --> for variavel from inicio to final: code fend
Fim de frase --> ;


Inicio de programa --> begin
Fim de programa --> end


Atribuição de valores/variavel --> =


Operadores lógicos -->  >=, <=, >, <, ==, !=


Declaração de variáveis --> int var 1  (tipo  nome)



estrutura 
saida de dados:
output("texto");
output("Olá mundo!")

entrada de dados:
input(variavel);
input(a);

declaração de variaveis:
tipo nome;
int a;

condição se:
if(condição)?
	codigo
@
	codigo
?;

if(a > b)?
	output(a);
@
	output(b);
?;

condição de repetição(faça enquanto):
do:
	codigo
done(condição);

do:
	a = a + 1;
done(a < 10);

condição de repetição(para):
for variavel from inicio to final: 
	code 
fend

for x from 0 to 10:
	a = a * x;
fend