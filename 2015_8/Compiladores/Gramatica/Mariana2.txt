Tipo de variavel --> Numerico


Entrada de dados --> input


Saida de dados --> output


Estrutura condicional --> if(condição)? se verdadeiro @ se falso?


Estrutura de repetição --> do: code done(condição)


Estrutura de repetição 2 --> for condição final (valor inicial, valor incremento): code fend
Fim de frase --> ;


Inicio de programa --> begin
Fim de programa --> end


Atribuição de valores/variavel --> =


Operadores lógicos -->  >=, <=, >, <, ==, !=


Declaração de variáveis --> int var 1  (tipo  nome)



estrutura 
saida de dados:
output("texto");
output("Olá mundo!")

entrada de dados:
input(variavel);
input(a);

declaração de variaveis:
tipo nome;
int a;

condição se:
if(condição)?
	codigo
@
	codigo
?;

if(a > b)?
	output(a);
@
	output(b);
?;

condição de repetição(faça enquanto):
do:
	codigo
done(condição);

do:
	a = a + 1;
done(a < 10);

condição de repetição(para):
for condição final (valor inicial, valor incremento): 
	code 
fend

for x >= 1 (0,-1): 
	a = a * x;
fend