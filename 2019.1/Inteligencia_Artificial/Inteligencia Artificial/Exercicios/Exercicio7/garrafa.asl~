// Agent garrafa in project exercicio04.mas2j

/*4) Crie uma garrafa térmica inteligente (chá ou café), onde a garrafa não permite que o 
liquido acabe, informando a um responsável estado da garrafa. */

acabou.
liquido(0).
!encher.
/* Plans */
+!encher: acabou & liquido(S) & S<=50 <-
	.print("Colocando cha ...");
	-+liquido(S+1);
	.print(S);
	.wait(100);
	!encher.

+!encher: liquido(S) & + S>50 <-
	.print("Esta cheio.");
	-acabou.
