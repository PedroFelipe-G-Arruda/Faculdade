#include <Programa_Pic12f675.h>

void main()
{
   setup_adc_ports(sAN0);
   setup_adc(ADC_CLOCK_DIV_2);

   while(TRUE)
   {
   
   output_HIGH(PIN_A1);
   delay_ms(50);
   output_LOW(PIN_A1);
   delay_ms(50);

      //TODO: User Code
   }

}
